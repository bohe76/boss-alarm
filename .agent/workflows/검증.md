---
description: 프로젝트의 코드 품질(Lint)과 로직(Test)을 검증합니다.
---

1. `GEMINI.md`의 **[실행 정책]**에 따라 현재 변경된 파일들에 `.js` 등 로직 파일이 포함되어 있는지 확인합니다.
// turbo
2. `npm run lint` 명령어를 실행하여 코드 스타일 및 문법을 검사합니다.
3. **GEMINI 정책 준수:** 로직 수정(`.js`)이 포함된 경우에만 `npm test`를 실행합니다. 정적 파일(.md, .html, .css)만 변경된 경우 테스트는 생략(Skip)합니다.
4. 검사 결과를 요약 보고하고 대기합니다.
