---
description: 린트 에러가 0이 될 때까지 자동으로 분석하고 수정합니다.
---

1. `npm run lint` 명령어를 실행하여 에러 및 경고 목록을 확보합니다.
2. 에러가 발견되면, `GEMINI.md`의 **[에이전트 작업 원칙]**에 따라 사용자 승인 없이 즉시 해당 파일을 분석하여 수정을 진행합니다.
3. 수정을 마친 후 다시 `npm run lint`를 실행하여 잔여 에러를 확인합니다.
4. 에러가 0개가 될 때까지 1~3단계를 반복합니다.
5. 모든 에러가 해결되면 결과를 보고하고 대기합니다.
