# 보탐 계산기 (FRD)

## 메타
- **요구사항 ID**: FRD-CAL-001
- **버전**: v3.0.0
- **우선순위**: P1
- **상태**: 구현 완료
- **관련 코드**: `src/screens/calculator.js`, `src/data-managers.js`
- **관련 PRD**: [PRD §계산기](../prd/product_requirements.md#계산기)

---

## 1. 개요

'보탐 계산기' 화면은 "젠 계산기"와 "광 계산기" 두 가지 주요 기능을 제공하여, 사용자가 보스 리젠 시간 및 '광' 타이밍을 계산하고 관리할 수 있도록 합니다. 이 화면은 하나의 탭(탭 전환 없음)으로 구성되어 두 계산기를 동시에 표시하며, 사용자 편의를 위한 기록 저장 및 관리 기능도 포함합니다.

---

## 2. 사용자 시나리오

- **시나리오 1**: 사용자가 남은 시간 `1230`을 입력하면 → 젠 시간이 `HH:MM` 형식으로 자동 계산된다.
- **시나리오 2**: 사용자가 젠 계산기에서 보스 선택 후 "업데이트" 클릭 → 해당 보스의 스케줄이 정밀한 Date 객체 기준으로 업데이트된다.
- **시나리오 3**: 광 계산기에서 "시작" → "광" → "잡힘" 순서로 클릭하면 → 경과/광/잡힘 시간이 계산되고 보스 이름 입력 프롬프트가 표시된다.
- **시나리오 4**: 보스 이름 입력 후 확인하면 → 광 계산 목록에 기록이 저장된다.
- **시나리오 5**: "목록 초기화" 클릭 시 → 저장된 모든 광 계산 기록이 삭제된다.

---

## 3. 기능 요구사항 (FR)

| ID | 요구사항 | 우선순위 | 상태 |
|----|---------|---------|------|
| FR-CAL-001 | 남은 시간 입력 시 젠 시간을 HH:MM:SS 형식으로 자동 계산·표시한다 | P0 | ✅ |
| FR-CAL-002 | HH:MM:SS, HH:MM, HHMM, HHMMSS, HMM 등 다양한 입력 형식을 지원한다 | P0 | ✅ |
| FR-CAL-003 | 정밀 Date 객체(lastCalculatedBossTime)를 메모리에 보존하여 텍스트 재파싱 없이 보스 시간을 업데이트한다 (v2.17.4) | P0 | ✅ |
| FR-CAL-004 | Single Anchor Principle: 동일 보스 이름의 기존 인스턴스를 모두 제거한 후 새 시간을 주입한다 (v2.17.6) | P0 | ✅ |
| FR-CAL-005 | 보스 선택 드롭다운에 현재 시간 이후 예정된 보스만 표시한다 | P0 | ✅ |
| FR-CAL-006 | 광 계산기 "시작" 버튼 클릭 시 스톱워치를 시작하고 버튼을 즉시 비활성화한다 | P0 | ✅ |
| FR-CAL-007 | "광" 버튼 클릭 시 해당 시점을 광 시간으로 기록하고 예상 잡힘 시간을 카운트다운한다 | P0 | ✅ |
| FR-CAL-008 | 예상 시간이 00:00이 되면 오버 시간으로 전환하여 증가 카운트를 표시한다 | P0 | ✅ |
| FR-CAL-009 | "잡힘" 버튼 클릭 시 보스 이름 입력 프롬프트를 표시하고 기록을 저장한다 | P0 | ✅ |
| FR-CAL-010 | 프롬프트 "취소" 시 기록을 저장하지 않고 로그에 취소 메시지를 남긴다 | P1 | ✅ |
| FR-CAL-011 | 광 계산 기록을 로컬 스토리지에 영구 저장하고 화면 진입 시 자동 로드한다 | P0 | ✅ |
| FR-CAL-012 | "목록 초기화" 버튼으로 모든 광 계산 기록을 삭제한다 | P1 | ✅ |
| FR-CAL-013 | 화면 진입 시 광 계산기 모든 상태를 초기화한다 | P0 | ✅ |

---

## 4. 수용 기준 (Acceptance Criteria)

- **AC-001**: Given 남은 시간 입력 필드에 `1230` 입력 / When 값이 변경됨 / Then bossAppearanceTimeDisplay에 `HH:MM:SS` 형식으로 젠 시간이 표시된다.
- **AC-002**: Given 젠 계산기에서 보스를 선택하고 업데이트 클릭 / When 처리됨 / Then 화면에 표시된 텍스트가 아닌 메모리의 정밀 Date 객체를 사용하여 보스 시간이 업데이트된다.
- **AC-003**: Given 동일 이름의 보스가 데이터셋에 여러 개 존재 / When 젠 계산기에서 해당 보스 업데이트 / Then 기존 인스턴스를 모두 제거한 후 새 시간 하나만 주입된다.
- **AC-004**: Given 광 계산기 "시작" 클릭 / When 스톱워치 시작 / Then "시작" 버튼이 즉시 비활성화되고 "광", "잡힘" 버튼이 활성화된다.
- **AC-005**: Given 광 계산기 "잡힘" 클릭 / When 처리됨 / Then 보스 이름 입력 프롬프트가 표시되고, 입력 확인 시 광 계산 목록에 기록이 추가된다.
- **AC-006**: Given 화면 진입 / When 이전에 저장된 광 계산 기록이 있을 때 / Then crazySavedList에 기록이 자동으로 로드·표시된다.

---

## 5. 의존성

- **데이터**: 로컬 스토리지(광 계산 기록), `BossDataManager`(보스 목록 조회)
- **모듈**: `BossDataManager.getBossSchedule()` (보스 드롭다운 목록), `LocalStorageManager` (기록 저장/로드), `logger.js` (취소 로그)
- **외부**: `window.speechSynthesis` (해당 없음, 이 화면 직접 사용 안 함)

---

## 6. 비기능 요구사항

- **정밀도**: 젠 계산기 업데이트는 텍스트 재파싱이 아닌 메모리 내 정밀 `Date` 객체를 사용하여 시간 오차를 방지한다 (v2.17.4 정밀 날짜/시간 보존 엔진).
- **데이터 중복 방지**: Single Anchor Principle(v2.17.6)으로 동일 보스 중복 인스턴스 생성을 원천 차단한다.

---

## 7. 미해결 이슈 / TODO

- [`issue-013`](../issues/issue-013-crazy-calculator-reset.md): 광 계산기 리셋 관련 이슈 검토
- [`issue-031`](../issues/issue-031-zen-calculator-duplicate-boss-bug.md): 젠 계산기 중복 보스 버그 (Single Anchor Principle 적용으로 해결됨, 모니터링 지속)
- 잡힘 버튼의 `prompt()` 사용 → issue-035(배너 UI) 해결 후 개선 검토

---

## 8. 동작 명세

### 8.1. 기능 접근
사용자는 좌측 사이드바 메뉴 또는 모바일 환경의 '더보기' 메뉴에서 '계산기' 메뉴 항목을 클릭하여 '보탐 계산기' 화면으로 이동합니다.

### 8.2. 젠 계산기 (Zen Calculator)
- **목적:** 보스의 남은 시간을 입력하여 정확한 출현 시간을 계산하고, 필요에 따라 현재 보스 목록의 젠 시간을 업데이트하는 기능.
- **남은 시간 입력:**
    - 텍스트 입력 필드(`remainingTimeInput`)에 보스 리젠까지 남은 시간을 입력합니다.
    - **입력 형식:** 다음 형식을 지원합니다.
        - 표준 시계 시간: `HH:MM:SS`, `HH:MM`
        - 숫자 형식 (표준 시계 시간): `HHMM` (예: `1305`는 13시 05분), `HHMMSS` (예: `130530`은 13시 05분 30초), `HMM` (예: `315`는 3시 15분)
- **젠 시간 표시:** 입력된 남은 시간을 바탕으로 계산된 보스 출현 시간(젠 시간)이 `HH:MM:SS` 형식으로 `bossAppearanceTimeDisplay` 영역에 자동으로 표시됩니다.
- **보스 목록 업데이트:**
    - **보스 선택 드롭다운:** 현재 `BossDataManager`에 등록된 보스 목록 중 현재 시간보다 미래에 예정된 보스들이 드롭다운(`bossSelectionDropdown`)에 표시됩니다.
    - **정밀 날짜/시간 보존 엔진 (v2.17.4)**: 화면에 표시된 텍스트(`HH:MM:SS`)를 파싱하지 않고, 계산 시점에 메모리에 저장해 둔 **정밀한 `Date` 객체(`lastCalculatedBossTime`)를 직접 참조**하여 업데이트합니다.
    - **Single Anchor Principle (v2.17.6)**: 보스 시간 업데이트 시, '동일한 보스 이름'을 가진 기존의 모든 인스턴스를 데이터셋에서 먼저 제거(Filtering)한 후 새롭게 계산된 시간을 주입합니다. 이를 통해 계산기 사용 시 동일 보스가 중복 생성되는 버그를 원천 차단하고, 항상 최신의 단일 앵커 포인트만 유지되도록 보장합니다.

### 8.3. 광 계산기 (Crazy Calculator)
- **목적:** '광'이 출현하는 보스의 타이밍을 측정하고, 광/잡힘 시간을 기록하며, 예상 잡힘 시간을 계산하는 기능.
- **스톱워치 기능:**
    - **'시작' 버튼 (`crazyStartButton`):**
        - **초기 상태:** 계산기 화면 진입 시 '시작' 버튼은 **활성화**되어 있습니다.
        - **'시작' 클릭 시:** 스톱워치를 시작하고 `경과 시간`(`crazyStopwatchDisplay`)이 00:00부터 1초 단위로 증가합니다. '시작' 버튼은 **즉시 비활성화**됩니다. '광' 및 '잡힘' 버튼이 **활성화**됩니다.
        - **스톱워치 정지:** '시작' 버튼 자체로는 스톱워치를 정지시킬 수 없습니다. 스톱워치를 정지하려면 '잡힘' 버튼을 클릭하여 전체 계산 과정을 완료해야 합니다.
        - **스톱워치 재개:** '잡힘' 버튼을 클릭하여 계산기를 초기화한 후, 다시 '시작' 버튼을 누르면 스톱워치가 00:00부터 시작됩니다.
    - **경과 시간 표시:** `MM:SS` 형식으로 현재까지의 경과 시간을 표시합니다.
- **'광' 버튼 (`crazyGwangButton`):**
    - 스톱워치 실행 중에 '광' 버튼을 클릭하면, 해당 시점의 경과 시간을 '광 시간'으로 기록합니다.
    - '광' 버튼은 '잡힘' 버튼을 누르기 전까지 활성화 상태를 유지하며, 여러 번 누를 수 있습니다.
    - 버튼을 누를 때마다 마지막으로 누른 시점을 기준으로 '예상 시간'이 다시 계산되어 `crazyExpectedTimeDisplay`에 `MM:SS` 형식으로 카운트다운됩니다.
    - '예상 시간'이 00:00이 되면, '오버 시간'으로 전환되어 1초 단위로 시간이 증가하며 `crazyExpectedTimeDisplay`에 표시되고, 시각적으로 '오버 시간'임을 나타내는 변화가 적용됩니다.
- **'잡힘' 버튼 (`crazyCaptureButton`):**
    - '잡힘' 버튼 클릭 시, **사용자에게 기록할 '보스 이름'을 입력하라는 프롬프트 창이 표시됩니다.**
    - 사용자가 프롬프트 창에 보스 이름을 입력하고 '확인'을 누르면 (또는 입력 없이 '확인'을 누르면), 현재 스톱워치 경과 시간과 기록된 '광 시간', 그리고 예상/오버 시간을 바탕으로 계산된 정보가 '광 계산 목록'에 기록됩니다.
    - 만약 사용자가 프롬프트 창을 '취소'하면 기록은 저장되지 않고 로그에 취소 메시지가 남습니다.
    - 기록 후에는 '광 계산기'의 모든 상태가 초기화되고, '광' 버튼도 비활성화됩니다.
- **최근 계산 결과 표시:** "잡힘" 버튼을 눌러 계산된 직후의 '광', '잡힘', '총 시간' 정보가 임시로 `crazyTempResults` 영역에 표시됩니다.
- **기록 저장 및 불러오기:**
    - '잡힘' 버튼을 통해 기록된 '광 계산 목록'은 브라우저의 로컬 스토리지에 영구적으로 저장됩니다.
    - '보탐 계산기' 화면으로 이동 시 저장된 기록이 자동으로 로드되어 '광 계산 목록' 섹션(`crazySavedList`)에 표시됩니다.
    - '목록 초기화' 버튼(`clearCrazyRecordsButton`) 클릭 시 저장된 모든 기록을 삭제합니다.
- **데이터 초기화:** '보탐 계산기' 화면으로 이동 시 '광 계산기'의 모든 상태(스톱워치, 표시 시간 등)가 초기화됩니다.
