# 버전 정보 (FRD)

## 메타
- **요구사항 ID**: FRD-VER-001
- **버전**: v3.0.0
- **우선순위**: P2
- **상태**: 구현 완료
- **관련 코드**: `src/screens/version-info.js`
- **관련 PRD**: [PRD §버전 정보](../prd/product_requirements.md#버전-정보)

---

## 1. 개요

'버전 정보' 화면은 애플리케이션의 릴리즈 노트(Release Notes)를 표시하여 사용자가 각 버전별 변경 사항, 새로운 기능, 수정 사항 등을 확인할 수 있도록 합니다. 이 정보는 `data/version_history.json` 파일에서 동적으로 로드되어 표시됩니다.

---

## 2. 사용자 시나리오

- **시나리오 1**: 사용자가 '릴리즈 노트' 메뉴를 클릭하면 → 가장 최신 버전 정보가 펼쳐진 상태로 표시된다.
- **시나리오 2**: 사용자가 이전 버전 항목을 클릭하면 → 아코디언이 펼쳐지고 해당 버전의 변경 사항이 표시된다.
- **시나리오 3**: JSON 로드에 실패하면 → "버전 정보를 불러오는 데 실패했습니다." 메시지가 표시된다.

---

## 3. 기능 요구사항 (FR)

| ID | 요구사항 | 우선순위 | 상태 |
|----|---------|---------|------|
| FR-VER-001 | data/version_history.json에서 버전 정보를 비동기로 로드한다 | P0 | ✅ |
| FR-VER-002 | 각 버전을 아코디언 형태로 표시하며 기본적으로 가장 최신 버전을 펼친다 | P0 | ✅ |
| FR-VER-003 | 각 버전에 fullVersion, releaseDate, changes 목록을 표시한다 | P0 | ✅ |
| FR-VER-004 | changes 항목에 type(feat/fix 등), description, details(선택)를 표시한다 | P1 | ✅ |
| FR-VER-005 | JSON 로드 실패 또는 형식 오류 시 오류 메시지를 표시하고 로그에 기록한다 | P0 | ✅ |

---

## 4. 수용 기준 (Acceptance Criteria)

- **AC-001**: Given '버전 정보' 화면 진입 / When 화면 렌더링 / Then 가장 최신 버전(첫 번째 항목)이 펼쳐진 상태로 표시된다.
- **AC-002**: Given version_history.json 정상 로드 / When 화면 렌더링 / Then fullVersion, releaseDate, changes 목록이 올바르게 표시된다.
- **AC-003**: Given version_history.json 로드 실패 / When 화면 진입 / Then "버전 정보를 불러오는 데 실패했습니다." 메시지가 표시된다.

---

## 5. 의존성

- **데이터 파일**: `data/version_history.json` (버전별 릴리즈 노트)
- **모듈**: `logger.js` (로드 오류 기록)
- **외부**: 없음

---

## 6. 비기능 요구사항

- **유지보수성**: 버전 정보가 JSON 파일로 분리되어 코드 수정 없이 새 버전 엔트리를 추가할 수 있다.
- **오류 처리**: 파일 로드 실패 시 앱이 중단되지 않고 graceful하게 오류 메시지를 표시한다.

---

## 7. 미해결 이슈 / TODO

- [`issue-034`](../issues/issue-034-version-bump-to-v3.md): ✅ 해결 완료 (version_history.json에 v3.0.0 엔트리 추가)
- 변경 유형(feat/fix)별 시각적 구분(뱃지, 아이콘) 개선 검토 (P2)

---

## 8. 동작 명세

### 8.1. 기능 접근
사용자는 좌측 사이드바 메뉴 또는 모바일 환경의 '더보기' 메뉴에서 '릴리즈 노트' 메뉴 항목을 클릭하여 '버전 정보' 화면으로 이동합니다.

### 8.2. 버전 정보 표시
- **정보 출처:** `data/version_history.json` 파일에 정의된 JSON 형식의 데이터를 비동기적으로 로드하여 사용합니다.
- **표시 형식:** 각 버전 정보는 아코디언(Accordion) 형태로 표시되며, 기본적으로 가장 최신 버전의 정보가 펼쳐져(open) 사용자에게 바로 보입니다.
- **내용 구성:** 각 버전은 전체 버전 번호(`fullVersion`), 릴리즈 날짜(`releaseDate`), 그리고 변경 사항 목록(`changes`)을 포함합니다.
    - `changes` 목록의 각 항목은 변경 유형(`type`, 예: `feat`, `fix`), 간략한 설명(`description`), 그리고 상세 설명(`details`, 선택 사항)을 가집니다.
- **오류 처리:** `data/version_history.json` 파일을 로드하는 데 실패하거나, 파일의 JSON 형식이 올바르지 않을 경우, 화면에는 "버전 정보를 불러오는 데 실패했습니다."라는 메시지가 표시되고 관련 오류는 애플리케이션 '로그'에 기록됩니다.
