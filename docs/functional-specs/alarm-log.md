# 알림 로그 (FRD)

## 메타
- **요구사항 ID**: FRD-ALG-001
- **버전**: v3.0.0
- **우선순위**: P1
- **상태**: 구현 완료
- **관련 코드**: `src/screens/alarm-log.js`, `src/logger.js`, `src/data-managers.js`
- **관련 PRD**: [PRD §알림 로그](../prd/product_requirements.md#알림-로그)

---

## 1. 개요

'알림 로그' 화면은 애플리케이션 내에서 발생한 모든 알림 메시지 및 시스템 메시지를 시간 순으로 기록하고 표시하는 기능을 제공합니다. 사용자는 로그 목록을 필터링하여 최근 메시지만 보거나 전체 메시지를 볼 수 있으며, 이 설정은 브라우저를 다시 시작해도 유지됩니다.

---

## 2. 사용자 시나리오

- **시나리오 1**: 사용자가 '로그' 메뉴를 클릭하면 → 발생한 모든 알림 및 시스템 메시지가 시간 순으로 표시된다.
- **시나리오 2**: '15개 보기' 토글을 활성화하면 → 최근 15개 로그만 표시되고, 비활성화하면 전체 로그가 표시된다.
- **시나리오 3**: 알림이 발생하면 → log-updated 이벤트가 수신되어 로그 목록이 즉시 갱신된다.
- **시나리오 4**: 중요 알림이 발생하면 → 해당 로그 항목이 시각적으로 강조(굵게 표시)된다.

---

## 3. 기능 요구사항 (FR)

| ID | 요구사항 | 우선순위 | 상태 |
|----|---------|---------|------|
| FR-ALG-001 | 각 로그 항목에 발생 시각([HH:MM:SS])과 메시지 내용을 표시한다 | P0 | ✅ |
| FR-ALG-002 | isImportant: true로 기록된 로그는 시각적으로 강조하여 표시한다 | P0 | ✅ |
| FR-ALG-003 | 로그 목록이 화면을 넘어갈 경우 자동으로 스크롤바를 생성한다 | P0 | ✅ |
| FR-ALG-004 | 새 로그 추가 시 자동으로 최신 로그가 보이도록 스크롤한다 | P0 | ✅ |
| FR-ALG-005 | '15개 보기' 토글로 최근 15개/전체 보기 모드를 전환한다 | P1 | ✅ |
| FR-ALG-006 | '15개 보기' 설정 상태를 로컬 스토리지에 저장하여 재시작 후 복원한다 | P1 | ✅ |
| FR-ALG-007 | EventBus의 log-updated 이벤트 수신 시 로그 목록을 즉시 갱신한다 | P0 | ✅ |

---

## 4. 수용 기준 (Acceptance Criteria)

- **AC-001**: Given 알림이 발생한 상태 / When '로그' 화면 진입 / Then 모든 로그 항목이 `[HH:MM:SS] 메시지` 형식으로 시간순 표시된다.
- **AC-002**: Given logger.js에서 isImportant: true로 기록된 항목 / When 로그 목록 렌더링 / Then 해당 항목이 시각적으로 구분되어 표시된다.
- **AC-003**: Given '15개 보기' 토글이 활성화된 상태 / When 로그 목록 렌더링 / Then 최근 15개 항목만 표시된다.
- **AC-004**: Given '15개 보기' 토글 상태를 변경 / When 앱을 재시작 / Then 마지막 설정 상태가 복원된다.
- **AC-005**: Given log-updated 이벤트 발생 / When 알림 로그 화면이 활성화된 상태 / Then 로그 목록이 즉시 갱신되고 최신 항목으로 스크롤된다.

---

## 5. 의존성

- **모듈**: `logger.js` (`log()` 함수, `isImportant` 플래그), `EventBus` (`log-updated` 이벤트 구독), `LocalStorageManager` (15개 보기 상태 저장)
- **연관 화면**: 대시보드(최근 3개 로그 표시), 알림 로그(전체 로그 표시) — 동일 `log-updated` 이벤트 공유

---

## 6. 비기능 요구사항

- **실시간성**: `EventBus` 기반 이벤트 구독으로 로그 추가 즉시 UI가 갱신된다.
- **접근성**: 중요 로그 강조는 색상 외에도 시각적 구분 요소(굵기 등)를 사용한다(TBD).

---

## 7. 미해결 이슈 / TODO

- [`issue-011`](../issues/issue-011-logging-refactoring.md): 로깅 시스템 리팩토링 (logger.js 구조 개선 검토)
- 로그 항목 삭제/초기화 기능 미제공 — 향후 추가 검토(P2)

---

## 8. 동작 명세

### 8.1. 기능 접근
사용자는 좌측 사이드바 메뉴 또는 모바일 환경의 '더보기' 메뉴에서 '로그' 메뉴 항목을 클릭하여 '알림 로그' 화면으로 이동합니다.

### 8.2. 로그 목록 표시
- **정보 내용:** 각 로그 항목은 메시지 발생 시각(`[HH:MM:SS]`)과 메시지 내용을 포함합니다.
- **시각적 강조:** 알림 시스템에서 발생한 중요 알림 메시지(`logger.js`에서 `isImportant: true`로 기록된 경우)는 로그 목록에서 시각적으로 강조되어 표시됩니다.
- **스크롤:** 로그 목록이 화면을 넘어갈 경우 자동으로 스크롤바가 생성되며, 새로운 로그가 추가되면 자동으로 가장 최근 로그가 보이도록 스크롤됩니다.

### 8.3. 로그 필터링 (15개 보기)
- **제어:** '알림 로그' 화면 우측 상단에 위치한 '15개 보기' 토글 버튼(`viewMoreLogsButton`)을 클릭하여 로그 표시 모드를 전환할 수 있습니다.
    - **활성화 (기본값):** 토글 버튼이 활성화된 상태에서는 가장 최근에 발생한 15개의 로그 메시지만 표시됩니다.
    - **비활성화:** 토글 버튼이 비활성화된 상태에서는 모든 로그 메시지가 표시됩니다.
- **상태 유지:** '15개 보기' 설정 상태는 브라우저의 로컬 스토리지에 저장되어 애플리케이션을 다시 시작해도 마지막 설정이 복원됩니다.

### 8.4. 로그 갱신
- **실시간 갱신:** 애플리케이션 내에서 `logger.js`의 `log()` 함수가 호출되어 새로운 로그 메시지가 추가될 때마다, `EventBus`를 통해 `log-updated` 이벤트가 발생하며, '알림 로그' 화면은 이 이벤트를 구독하여 실시간으로 로그 목록을 갱신합니다.
